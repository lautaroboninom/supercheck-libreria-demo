Branding activo de SuperCheck Librería:

- `libreria-pos-mark.svg`: marca principal usada en login, sidebar, favicon y PWA.

Si luego necesitan volver a generar iconos PNG, pueden mantener este SVG como base
y convertirlo a los tamanos requeridos para `public/icons/`.
